# 时间线分析报告（采集 vs 真机推理）

日期：2026-01-18  
数据来源：
- 采集端 timeline：`timeline_record_*.jsonl`
- 推理端 timeline：`inference_logs/timeline_*.jsonl`
- 汇总命令：
  ```bash
  python catkin_ws/src/carm_deploy/tools/analyze_timeline.py \
    --logs /home/lizh/rl-vla/timeline_record/timeline_record_*.jsonl /home/lizh/rl-vla/inference_logs/timeline_*.jsonl \
    --out ./summary.json
  ```

## 1. 目标与问题

目标：搞清楚真机部署时“观测 → 推理 → chunk → 控制下发”的时间语义，并对比数据采集时的时间语义，定位训练与推理（train–infer）时间线 gap 的主要来源；同时把 **action chunking** 的时间基准/目标时间（chunk targets）纳入分析。

本报告基于 `summary.json` 中的统计结果，解释各指标代表什么、当前系统的主要延迟在哪里、chunking 的时间轴如何落在真实系统上，并给出可操作的改进建议。

## 2. 系统时间线（按代码真实语义）

推理节点（`inference_ros.py`）关键时间点：

1) **obs 事件**
- `obs_stamp_ros`：相机/同步观测的 ROS 时间戳（来自 `RealEnvironment.get_observation()` 返回的 `obs['stamp']`）
- `t_obs_ready_sys`：推理线程拿到 `latest_obs` 的系统时间（`time.time()`）
- `delta_obs = t_obs_ready_sys - obs_stamp_ros`

2) **inference 事件**
- `t_infer_start` / `t_infer_end`：推理开始/结束的系统时间
- `inference_time = t_infer_end - t_infer_start`

3) **chunk 事件**
- `chunk_time_base` 配置：
  - `sys_time`（当前默认）：`chunk_base_time = time.time()`（在推理结束、动作转换与安全检查之后）
  - `obs_stamp`：`chunk_base_time = obs_stamp_ros`
- `delta_chunk_obs = chunk_base_time - obs_stamp_ros`
- `chunk_targets[i] = chunk_base_time + i * action_interval`
- 当前实现里 `action_interval` 固定为 `1/30`（约 33.33ms），因此 `pred_horizon=16` 时，chunk 覆盖 $0\sim0.5\,s$。

4) **control 事件**
- 控制线程每次循环用 `query_time=tm` 去 `ActionChunkManager.get_fused_action_with_meta(tm)` 拿融合动作
- `t_send_sys`：实际下发前记录的系统时间
- `control_lag = t_send_sys - query_time`
- `candidate_timestamps`：当前融合时刻用于插值/加权的候选轨迹时间戳集合
- `candidate_age = query_time - max(candidate_timestamps)`（注意：这个定义意味着“若为负数”，代表 `max(candidate_timestamps)` 在未来）
- `candidate_span = max(candidate_timestamps) - min(candidate_timestamps)`

## 3. 指标定义（来自 analyze_timeline.py）

- `delta_obs`：`t_obs_ready_sys - obs_stamp_ros`（obs / record_step 两类事件共同统计）
- `inference_time`：推理耗时
- `delta_chunk_obs`：`chunk_base_time - obs_stamp_ros`
- `chunk_len`：每次推理产生的 chunk 长度（= `pred_horizon`）
- `chunk_lead`：`chunk_targets[0] - chunk_base_time`（理论上应为 0）
- `chunk_target_offsets`：`chunk_targets[i] - chunk_base_time`（即 chunk 内部的时间偏移分布）
- `control_lag`：`t_send_sys - query_time`
- `candidate_age`：`query_time - max(candidate_timestamps)`
- `candidate_span`：`max(candidate_timestamps) - min(candidate_timestamps)`

## 4. 统计结果概览（来自 summary.json）

样本量：
- `obs`: 11791
- `inference`: 11791
- `chunk`: 11791
- `control`: 11192
- `record_step`: 1358

关键分布（单位：秒）：

- `delta_obs`: mean 0.0446, p95 0.0625, p99 0.0695, max 0.0970
- `inference_time`: mean 0.0331, p95 0.0846, p99 0.0956, max 0.5087
- `delta_chunk_obs`: mean 0.0864, p95 0.1337, p99 0.2416, max 0.5693
- `control_lag`: mean 0.000307, p95 0.000441, p99 0.000592, max 0.00609
- `candidate_age`: mean -0.0299, p50 -0.0309, p95 -0.0233, max -0.00564
- `candidate_span`: mean 0.0265, p50 0.0274, p95 0.0322, max 0.0333
- `chunk_target_offsets`: min 0.0, p50 0.25, p95 0.5, max 0.5
- `chunk_len`: 恒为 16
- `chunk_lead`: 恒为 0
- `record_action_present_rate`: 1.0

## 5. 解读与结论

### 5.1 观测端延迟（delta_obs）是稳定的“固定成本”

`delta_obs` 均值约 44.6ms，p95 62.5ms，最大 97ms。

这通常来自：
- 相机曝光/ISP/USB 传输与驱动缓冲
- ROS message_filters/同步等待（虽然 slop 已收紧，但仍有排队）
- Python 侧解码/拷贝（cv_bridge）

结论：就算推理是 0ms，系统对“真实世界”的响应也至少滞后约 45ms（均值）。

### 5.2 推理耗时（inference_time）均值可用，但存在明显长尾

`inference_time` 均值约 33ms（接近 30Hz 的预算 33.3ms），但：
- p95 已到 84.6ms
- 最大值到 508ms

这意味着推理线程会偶发性卡顿/抖动（GPU 同步、数据搬运、CPU 争用、偶发的 kernel 选择或系统调度都可能导致）。

结论：**长尾推理**是造成“偶发动作对齐失败/控制表现发飘”的关键风险点。

### 5.3 chunk 基准时间相对观测的偏移（delta_chunk_obs）刻画了端到端反应延迟

当前 `chunk_time_base=sys_time` 时：
- `chunk_base_time` 是推理结束之后的 `time.time()`
- 因此 `delta_chunk_obs = chunk_base_time - obs_stamp_ros` 近似等于：

$$\underbrace{(t_{obs\_ready}-t_{obs\_stamp})}_{\Delta_{obs}} + \underbrace{t_{preprocess}+t_{infer}+t_{postprocess}}_{\Delta_{compute}}$$

统计上 `delta_chunk_obs` 均值 86.4ms，p95 133.7ms，最大 569ms。

对照：`delta_obs`（44.6ms） + `inference_time`（33.1ms）≈ 77.7ms，和 `delta_chunk_obs`（86.4ms）同量级，剩余差额可归因于图像 resize/归一化、动作转换、安全检查、线程调度等开销。

结论：在当前实现下，**每次推理产出的 chunk 的第一个目标时刻**，大约对应“相机拍到画面后 80–130ms（典型范围）才开始执行”的动作。

### 5.4 action chunking 的时间语义符合预期，但“未来对齐”很关键

`chunk_target_offsets` 分布为 $0\sim0.5\,s$，p50=0.25s，max=0.5s。
- 这与 `pred_horizon=16`、`action_interval=1/30s` 完全一致：偏移为 $i/30$，$i\in[0,15]$。

`candidate_age` 均值约 -29.9ms（负数）：
- 由于定义是 `query_time - max(candidate_timestamps)`
- 负数意味着 **融合时刻 query_time 总能拿到“略在未来”的候选时间戳**（未来约 30ms）

这通常是好事：
- 控制线程以高频（~200Hz）查询动作
- chunk 提供 30Hz 的离散时间点
- 让融合点落在“略向前看”的候选上，有助于减少滞后、提高平滑性

同时 `candidate_span` 均值 26.5ms，接近 30Hz 的一个周期（33ms），说明融合时通常在相邻的少量候选点之间插值/加权，这是符合预期的。

结论：chunking 的内部时间轴是健康的；真正决定闭环表现的是 **chunk_base_time 相对 obs 的放置** 以及 **推理长尾**。

### 5.5 控制下发延迟（control_lag）极小

`control_lag` 均值 0.307ms，p99 0.592ms。

结论：SDK 下发/控制线程本身不是瓶颈；主要问题不在“有没有发出去”，而在“发出去的动作对应多旧的观测”。

## 6. 训练–推理 gap 的核心来源（结合本次统计）

从时间线角度，gap 主要来自两类不匹配：

1) **观测延迟 + 推理耗时带来的“基于旧观测决策”**
- 典型情况下：动作开始执行时，观测已滞后 80–130ms
- 如果训练数据隐含的是“动作≈观测同步”（或只滞后很小），部署会显著退化

2) **推理耗时长尾引入的不确定性（jitter）**
- p95 84ms、max 508ms 会导致 chunk_base_time 偶发性跳变
- 这会让 chunking 的时间对齐不稳定，即使平均值还不错

## 7. 可执行改进建议（按优先级）

### 7.1 先解决推理长尾（优先级最高）

目标：把 `inference_time` 的 p95/p99 拉回到可控范围（例如 p95 < 50ms）。

建议排查/优化方向：
- 确保推理全程无不必要的 CPU↔GPU 同步（例如 `.cpu().numpy()` 只在必要时做）
- 固定输入尺寸与 batch，避免触发 cudnn 算法反复选择
- 运行时减少系统干扰：绑定 CPU 亲和性/提升进程优先级/避免同时跑重负载任务
- 如允许：记录 GPU 利用率与显存占用，定位是否发生显存抖动或 page fault

验证方式：重复跑 timeline，关注 `inference_time.max/p99` 是否显著下降。

### 7.2 明确 chunk_base_time 的“语义选择”（sys_time vs obs_stamp）

当前 `sys_time` 的优点：
- `chunk_targets[0]` 立刻从“当前时刻”开始，执行时间语义直观

但它的代价：
- chunk 是基于 **滞后的观测** 生成的，第一帧动作等价于“用旧画面做反应”

可选策略（需要结合控制稳定性做 A/B）：

- 策略 A：继续用 `sys_time`，但在训练/数据侧显式建模延迟
  - 例如训练时对齐为：用 $t$ 的观测预测 $t+\tau$ 的动作（$\tau\approx 80\sim130ms$）
  - 或做 latency augmentation（随机延迟扰动）增强鲁棒性

- 策略 B：使用 `obs_stamp`，并显式加一个补偿 offset（推荐不要直接把 base=obs_stamp 原封不动用）
  - 直觉：`chunk_base_time = obs_stamp_ros + \tau_comp`
  - 其中 $\tau_comp$ 可先取 `delta_chunk_obs` 的 p50 或 p95（比如 0.08–0.13s）
  - 这样 chunk 的“时间轴”会更贴近观测语义，同时避免把目标时刻放到过去

> 备注：当前代码支持 `--chunk_time_base obs_stamp`，但**没有**内置 `+offset`，如果要走策略 B，建议加一个参数化 offset。

### 7.3 为“采集端 action 时间语义”补齐对比指标（建议补做）

当前统计里，采集端只有：
- `record_step.delta_obs`
- `record_action_present_rate`

但缺少“动作相对观测的延迟”（例如：`t_action_query_sys - obs_stamp_ros`）的统计，因此很难定量回答：
- 采集数据里 action 到底对应同一帧观测、下一帧观测，还是更晚？

建议后续把 `record_step` 里已有的 `t_action_query_sys` 纳入 analyze：
- 新增 `delta_record_action_obs = t_action_query_sys - obs_stamp_ros`
- 这样能直接把“采集 action 相对 obs 的时间语义”与 `delta_chunk_obs` 做对齐比较

## 8. 下一步验证清单（建议按顺序）

1) 固定一次基线：在当前配置下重复采样 2–3 次，确认统计稳定性
2) 针对推理长尾做一次优化/隔离后复测：重点看 `inference_time.max/p99`
3) 若要动 chunk_base_time：做 A/B（`sys_time` vs `obs_stamp+offset`），复测 `candidate_age` 与任务成功率
4) 增补采集端 action 对齐指标（`delta_record_action_obs`），形成“训练数据语义 vs 部署语义”的闭环对比

---

附：本报告直接引用的汇总文件：`/home/lizh/rl-vla/summary.json`。
